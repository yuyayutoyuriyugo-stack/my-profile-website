* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
  font-family: "Segoe UI", sans-serif;
}

body {
  background: #f4f8fc;
  color: #333;
  line-height: 1.6;
}

header {
  background: linear-gradient(90deg, #0b3c5d, #1e81ce);
}

.navbar {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 1rem 2rem;
}

.logo {
  color: #fff;
  font-size: 1.5rem;
  font-weight: bold;
}

.nav-links {
  list-style: none;
  display: flex;
  gap: 1.2rem;
}

.nav-links a {
  color: #fff;
  text-decoration: none;
}

.hamburger {
  display: none;
  font-size: 1.8rem;
  color: #fff;
  cursor: pointer;
}

.hero {
  background: linear-gradient(135deg, #1e81ce, #5fa8ff);
  color: #fff;
  text-align: center;
  padding: 4rem 2rem;
}

.section {
  max-width: 1100px;
  margin: auto;
  padding: 3rem 2rem;
}

.section.alt {
  background: #eaf2fb;
}

.skills {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  margin-top: 1rem;
}

.project-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1.5rem;
}

.project-card {
  background: #fff;
  padding: 1.5rem;
  border-radius: 8px;
  box-shadow: 0 6px 12px rgba(0,0,0,0.1);
}

.project-card a {
  color: #1e81ce;
  font-weight: bold;
  text-decoration: none;
}

.social-links a {
  margin-right: 1rem;
  color: #1e81ce;
  text-decoration: none;
}

footer {
  background: #0b3c5d;
  color: #fff;
  text-align: center;
  padding: 1rem;
}

/* Mobile */
@media (max-width: 768px) {
  .nav-links {
    display: none;
    flex-direction: column;
    position: absolute;
    right: 0;
    top: 60px;
    background: #0b3c5d;
    width: 220px;
    padding: 1rem;
  }

  .nav-links.active {
    display: flex;
  }

  .hamburger {
    display: block;
  }
}
